# CI-only requirements — PyPI packages needed for tools/ and tests/
# Do NOT add ROS2 packages here (rclpy, geometry-msgs, etc.) — they are not on PyPI
# requirements.txt is the full pip freeze of the local ROS2 venv (dev reference only)
# Editing this file invalidates the Dockerfile's pip-install and colcon-build layers (it's
# COPYed in ahead of both) — the cheapest way to force a genuinely non-cached stage-2-arm64
# timing when you need one for comparison.
# Cold-build trigger 2026-07-13: prove the NVMe fresh-install runner registration end to
# end and re-measure the cold arm64 build (SD-era reference: 585s, run 29135219100).
pyyaml>=6.0
pandera>=0.32
pandas>=2.0
numpy>=1.26
pillow>=10.0
scipy>=1.12
streamlit>=1.33
plotly>=5.20
reportlab>=4.0
matplotlib>=3.8
anthropic>=0.25
ollama>=0.4
pymupdf>=1.24
pytest==8.3.2
pytest-cov>=5.0
python-dotenv>=1.0
pytest-timeout>=2.3
# pyserial: tools/pin_gimbal.py imports `serial` at module level with no rclpy
# dependency at all — its test (tests/test_pin_gimbal.py) is meant to run on bare
# stage-1-quality (ubuntu-latest, no ROS2), unlike esp32_driver.py's own serial
# usage (excluded from stage-1 for an unrelated reason: it needs rclpy). Missing
# here caused a real stage-1 collection failure (ModuleNotFoundError), 2026-08-11.
pyserial>=3.5
